#include <stdio.h>

short int isPrimeNumber (int n);

int main (int argc, char *arcgv[]) {
    int n;// number
    scanf ("%d", n);
    int count = 1;
    while (count <= n) {
        count++;
    }

    return 1;
}

short int isPrimeNumber (int n) {

    return 1;
}